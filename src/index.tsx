import { CodeEditorBase } from './CodeEditorBase';
import {
  EDITOR_THEME,
  LANGUAGE_TYPE,
  THEME_NAME,
} from './CodeEditorBase/config';

export { CodeEditorBase, EDITOR_THEME, LANGUAGE_TYPE, THEME_NAME };
